/**
 * Comparison presets. These are observed results and must not be normalized,
 * rescaled, reordered or rounded. All values are percentages on a 0-100 scale.
 */

/** Demo 2 — Scaling Comparison. */
export const SCALING_COMPARISON_CSV = `category,value
Scales Worse,25.5
Similar,29.7
Scales Better,35.4
`;

/** Demo 3 — Scaling Comparison at 0-3k gold deficit. */
export const SCALING_COMPARISON_0_3K_CSV = `category,value
Scales Worse,32.2
Similar,41.2
Scales Better,47.5
`;
